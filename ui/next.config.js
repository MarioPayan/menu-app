/** @type {import('next').NextConfig} */
const nextConfig = {
    images: { // TODO: Replace with the actual domain
      domains: ['placekitten.com', 'dummyimage.com', 'picsum.photos', 'dvzwo3mu4ucsq.cloudfront.net', 'storage.googleapis.com'],
    },
  };
export default nextConfig;
